{
  "name": "compute-node-ui",
  "version": "1.0.0",
  "description": "UI for Compute Node job management system",
  "main": "index.html",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "vite": "^6.3.6"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  },
  "dependencies": {
    "prettier": "^3.6.2"
  }
}
