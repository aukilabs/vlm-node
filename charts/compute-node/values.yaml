# Default values for compute-node
# This is a YAML-formatted file.

# Service Account configuration
serviceAccount:
  create: true
  name: ""
  annotations: {}

# Server configuration
server:
  enabled: true
  replicas: 1
  image:
    repository: 026987513085.dkr.ecr.us-east-1.amazonaws.com/task-timing-node-server
    tag: "latest"
    pullPolicy: Always
  resources:
    requests:
      memory: "256Mi"
      cpu: "250m"
    limits:
      memory: "512Mi"
      cpu: "500m"
  ports:
    main: 8080
    admin: 18190

# UI configuration
ui:
  enabled: true
  replicas: 1
  port: 3000
  image:
    repository: 026987513085.dkr.ecr.us-east-1.amazonaws.com/task-timing-node-ui
    tag: "latest"
    pullPolicy: Always
  resources:
    requests:
      memory: "128Mi"
      cpu: "100m"
    limits:
      memory: "256Mi"
      cpu: "200m"

# Database configuration
postgresql:
  # If true, the PostgreSQL dependency is enabled
  enabled: false
  url: "placeholder"
  global:
    # defaultStorageClass: ""
    postgresql:
      auth:
        username: compute-node
        password: compute-node
        database: compute-node
  primary:
    networkPolicy:
      enabled: false
    resources:
      limits:
        cpu: 1
        memory: 256Mi
        ephemeral-storage: 2Gi
      requests:
        cpu: 100m
        memory: 128Mi
        ephemeral-storage: 50Mi
  fullnameOverride: "compute-node-postgresql"

# Storage configuration
storage:
  enabled: true
  storageClass: "gp2"  # Default to gp2, can be overridden for other clouds
  size: "100Gi"
  accessMode: ReadWriteOnce
  mountPath: /data

# Ingress configuration
ingress:
  enabled: true
  annotations:
    kubernetes.io/ingress.class: alb
    alb.ingress.kubernetes.io/scheme: internet-facing
    alb.ingress.kubernetes.io/group.name: default
    alb.ingress.kubernetes.io/target-type: ip
    alb.ingress.kubernetes.io/listen-ports: '[{"HTTPS":443}]'
  hosts:
    - compute-node
  tls:
    - hosts:
        - compute-node

# Service configuration
service:
  type: ClusterIP
  annotations: {}

# Security configuration
security:
  createSecret: false
  existingSecretName: "task-timing-node"
  appKey: "changeme"
  appSecret: "changeme"

# Labels and annotations
labels: {}
annotations: {}
